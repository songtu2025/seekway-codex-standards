# Codex 验证与完成报告规范

制定验证方案、执行检查、判断完成状态或输出完成报告时，必须读取本文件。

## 1. 项目检查声明

项目 README 必须记录实际可执行的 Windows 和 Linux/CI 检查命令。命令可以不同，但必须覆盖相同能力；本规范不限定脚本名称和任务运行器。

检查命令不得安装依赖、修改文件或静默跳过必需检查。优先使用项目统一入口，否则按 README 逐项执行。

## 2. 结果状态

每项检查只能报告为：

- **通过**：命令实际执行且退出成功。
- **失败**：命令实际执行但未通过。
- **未配置**：受影响模块缺少必需命令或工具，不能标记完成。
- **不适用**：任务不影响该模块或项目不存在对应能力；必须说明原因。
- **未自动验证**：缺少自动化工具但已进行人工检查；必须说明方法和局限。

## 3. 最低验证能力

任务影响前端时必须覆盖：

- 格式检查。
- ESLint。
- TypeScript 类型检查。
- 自动化测试。
- 生产构建。
- 已安装组件库和图标库的版本兼容性与重复安装检查。

任务影响后端时必须覆盖：

- Ruff 格式和静态检查。
- 项目选定的 Python 类型检查器。
- pytest。
- 存在 Alembic 配置时执行迁移检查。

存在相应配置时，还须检查 Docker Compose 配置和禁止提交的文件。缺少必需命令或工具时不得静默跳过。

## 4. 任务专项验证

新增、修改或评审代码时，还必须使用项目已有工具检查：

- 本次涉及文件的职责和规模。
- 函数圈复杂度、认知复杂度、分支数、语句数、嵌套和参数数量。
- 代码重复、死代码和循环依赖。
- 源码和提交记录中的敏感信息；扫描结果不得显示真实命中值。

项目缺少工具时，不得擅自增加生产依赖；应人工检查并报告为“未自动验证”，不能记为自动化检查通过。

涉及界面时，必须按 `docs/web-ui-standard.md` 的验收清单验证目标视口、页面状态、键盘路径、焦点、对比度、溢出和横向滚动。

采用 Ant Design 基线时，还须验证根级 `ConfigProvider`、`App` 上下文和中文环境生效，主要图标来自 `@ant-design/icons`，没有第二套 UI 组件库，且自定义样式不依赖 Ant Design 内部 DOM 或类名。

## 5. 规范文件专项验证

新增、合并或升级 `AGENTS.md` 及其专项规范时，还必须检查：

1. 原有业务、架构、检查和禁止事项未因模板接入或精简而丢失。
2. 根规则、嵌套规则、`AGENTS.override.md` 和专项文档的作用域及优先关系明确。
3. 所有索引路径存在，并且每个索引都说明了准确的读取条件；未同步的专项规范没有残留索引。
4. 每条规则只有一个事实来源，没有冲突、失效或已由工具强制却仍逐条复述的内容。
5. 新项目根 `AGENTS.md` 不超过 100 行；现有项目超过目标时已说明原因和治理方式。
6. 适用的项目规则链未超过 Codex 当前配置的文档大小限制；接近限制时优先减少始终加载内容。
7. 项目 README 已记录公司规范基线版本、接入日期、项目差异和真实检查命令。
8. 存在目录规则时，从代表性工作目录核对 Codex 实际加载的来源和顺序。

## 6. 结果真实性与完成报告

必须如实报告：

1. 需求完成情况。
2. 修改文件及主要改动。
3. 职责、规模、复杂度和代码健康检查结果。
4. 新增或修改的测试。
5. 实际执行的检查命令及通过、失败、未配置、不适用和未自动验证结果。
6. 涉及界面时的视觉、状态、键盘和可访问性验证结果。
7. 数据库、配置、依赖和部署影响。
8. 已知风险和回滚建议。

不得为了通过检查而删除测试、修改正确的测试预期、关闭检查规则、忽略类型错误、吞掉异常或隐藏失败结果。

只有满足验收标准、未超出范围、必要检查通过、未隐藏未验证内容且未降低代码健康度时，任务才可标记完成。代码已生成、能运行或理论可行都不等于完成。
