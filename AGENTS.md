# Codex 项目开发规范

- 规范来源：Seekway Codex 开发规范
- 规范版本：V1.0.0
- 最后更新：2026-08-06
- 默认开发环境：Windows
- 代码托管平台：GitHub
- 生产环境：阿里云 ECS

本规范用于约束 Codex 在公司项目中的需求分析、方案设计、代码修改、测试验证和结果汇报行为。

本规范中的“必须”“不得”和“禁止”均为强制要求。

## 1. 技术基线

默认技术栈：

### 前端

- React
- TypeScript
- Vite

### 后端

- Python
- FastAPI
- Pydantic
- SQLAlchemy
- Alembic
- pytest
- Ruff

### 部署

- Docker
- Docker Compose
- Nginx
- 阿里云 ECS

未经明确批准，Codex 不得：

1. 更换前端或后端框架。
2. 更换数据库访问或迁移方案。
3. 改变现有部署方式。
4. 引入新的基础设施组件。
5. 新增生产依赖。

## 2. 标准项目结构

新项目默认采用前后端分离、同一仓库管理的结构：

```text
project-name/
├── AGENTS.md
├── README.md
├── .env.example
├── .gitignore
├── compose.yaml
├── docs/
├── frontend/
│   ├── package.json
│   ├── src/
│   └── tests/
├── backend/
│   ├── pyproject.toml
│   ├── alembic.ini
│   ├── app/
│   ├── migrations/
│   └── tests/
├── deploy/
├── scripts/
│   ├── check.ps1
│   └── check.sh
└── .github/
    ├── workflows/
    ├── ISSUE_TEMPLATE/
    ├── pull_request_template.md
    └── CODEOWNERS
```

Codex 必须遵守：

1. 前端代码放在 `frontend`。
2. 后端代码放在 `backend`。
3. 项目文档放在 `docs`。
4. 数据库迁移放在 `backend/migrations`。
5. 部署配置放在 `deploy` 或根目录明确位置。
6. GitHub 配置放在 `.github`。
7. 未经批准不得改变一级目录结构。
8. 项目不需要的二级目录可以省略。
9. 不得提交临时文件、日志、数据库文件和真实业务数据。

维护历史项目时，应优先保持现有结构和兼容性，不得仅为了符合模板而进行无业务收益的全量重构。

## 3. 开始开发前

Codex 在修改代码前必须：

1. 阅读根目录和相关子目录中的 `AGENTS.md`。
2. 阅读 README、需求文档、相关代码和测试。
3. 检查相关调用链和现有实现方式。
4. 复述需求和验收标准。
5. 列出计划修改的文件及修改内容。
6. 说明是否涉及数据库、认证、权限、配置、依赖或部署。
7. 识别潜在风险。
8. 给出测试和验证方案。

未完成上述分析前，不得直接修改代码。

需求不明确时，不得自行编造业务规则。

## 4. 修改范围

Codex 必须遵循最小修改原则。

Codex 必须：

1. 只修改完成当前任务所必需的文件。
2. 保持现有业务规则、接口和兼容性。
3. 优先复用已有代码和实现模式。
4. 新增或修改业务逻辑时补充必要测试。
5. 发现无关问题时只记录，不顺带修改。

Codex 不得：

1. 自行增加功能、页面、接口或业务规则。
2. 自行增加需求中没有的业务字段。
3. 进行与当前任务无关的重构。
4. 修改无关文件。
5. 擅自删除已有功能、接口、字段或数据。
6. 擅自修改公共模块、认证、权限或部署配置。
7. 为“以后可能使用”提前增加代码、字段或组件。
8. 在多个位置复制相同业务规则。

## 5. Python 与 FastAPI

Codex 生成或修改的 Python 代码必须：

1. 符合 PEP 8。
2. 使用 Ruff 进行格式和静态检查。
3. 使用清晰的英文变量名、函数名、类名和文件名。
4. 普通注释和 docstring 使用中文。
5. 为公共函数和复杂业务逻辑提供类型注解。
6. 保持 FastAPI 路由简洁。

后端基本职责关系：

```text
API Router → Service → Repository → Database
```

职责约定：

- `api`：接收请求、校验参数、调用服务、返回结果。
- `schemas`：定义请求和响应结构。
- `services`：处理业务规则和业务流程。
- `repositories`：处理数据库访问。
- `models`：定义数据库模型。
- `core`：管理配置、日志、安全等公共能力。

Codex 不得：

1. 在 FastAPI 路由中堆积复杂业务逻辑。
2. 在路由中直接编写大量数据库查询。
3. 使用裸 `except`。
4. 吞掉异常。
5. 使用拼音或无意义缩写命名。
6. 通过关闭 Ruff 或类型检查规则掩盖问题。

## 6. React 与 TypeScript

Codex 生成或修改的前端代码必须：

1. 使用 React 函数组件和 Hooks。
2. 遵循 React 和 TypeScript 主流开发规范。
3. 使用清晰的英文变量名、函数名、组件名和文件名。
4. 普通注释、JSDoc 和 TSDoc 使用中文。
5. 将 API 请求集中放在 Service 层。
6. 将复杂业务逻辑放入 Hook、Service 或业务模块。
7. 正确处理加载、失败、空数据和成功状态。

Codex 不得：

1. 滥用 `any`。
2. 使用 `@ts-ignore` 掩盖错误。
3. 无理由使用非空断言。
4. 将大量请求、状态和业务逻辑堆积在单个组件中。
5. 在多个位置重复维护同一份状态。
6. 使用前端逻辑代替后端权限校验。
7. 将金额、库存和权限等关键结果以前端计算结果为准。

## 7. 注释规范

注释应解释：

- 业务规则
- 设计原因
- 边界条件
- 特殊处理
- 兼容性要求
- 风险原因

不得添加简单重复代码含义的无效注释。

## 8. 数据库规范

Codex 涉及数据库时必须：

1. 使用 Alembic 管理结构变更。
2. 提供升级和回滚方案。
3. 明确说明受影响的表、字段和数据。
4. 保持代码和迁移脚本一致。
5. 对批量操作说明影响范围。

Codex 不得：

1. 直接修改生产数据库。
2. 手工修改生产表结构。
3. 擅自删除表、字段、索引或业务数据。
4. 擅自增加需求中没有的业务字段。
5. 未经确认增加系统字段、派生字段或管理字段。
6. 使用浮点数存储或计算金额。
7. 自动执行生产数据库迁移。

生产数据库迁移只能由部署负责人执行。

## 9. 验证要求

开发人员默认在 Windows 环境使用 Codex。

Codex 提供的本地命令必须兼容 Windows PowerShell。

推荐 Windows 检查入口：

```powershell
.\scripts\check.ps1
```

Linux、GitHub Actions 或服务器检查入口：

```bash
./scripts/check.sh
```

两套脚本的检查内容必须保持一致。

Codex 完成修改后，至少应执行：

### 前端

- 格式检查
- ESLint
- TypeScript 类型检查
- 自动化测试
- 生产构建

### 后端

- Ruff 格式检查
- Ruff 静态检查
- Python 类型检查
- pytest
- Alembic 迁移检查

### 通用

- Docker Compose 配置检查
- 敏感信息检查

Codex 必须如实报告：

1. 实际执行的命令。
2. 已通过的检查。
3. 失败的检查。
4. 未执行的检查。
5. 未执行的原因。

Codex 不得为了通过检查而：

- 删除测试
- 修改正确的测试预期
- 关闭检查规则
- 忽略类型错误
- 吞掉异常
- 隐藏失败结果

## 10. 安全与生产边界

Codex 不得读取、生成、提交或传播真实敏感信息，包括：

- 真实密码、Token、AccessKey、SecretKey 和私钥
- 生产环境变量的真实值
- 未脱敏的真实业务数据
- 包含真实敏感信息的数据库文件、日志、上传文件和导出结果

Codex 可以读取和处理：

- 认证相关的代码、字段名和配置结构
- 不包含真实凭据的 `.env.example` 和占位值
- 测试数据、模拟数据和已经脱敏的数据
- 用户明确提供且不包含真实敏感信息的需求附件、示例文件和设计资料

发现真实敏感信息时，应停止处理相关内容并提醒用户，不得在回复中复述其值。

## 11. 项目文档

Codex 创建或维护项目时，至少必须维护：

### README.md

应包含：

- 项目用途和适用范围
- 明确不做的事项
- 技术栈和系统架构
- 关键业务规则
- 本地启动方式
- 测试和构建方式
- 部署文档入口

### AGENTS.md

应包含：

- 项目业务边界
- Codex 修改规则
- 禁止修改的内容
- 必须执行的检查
- 数据和 Git 安全要求

重要业务规则不得只存在于聊天记录或个人记忆中。

## 12. 完成报告

Codex 完成任务后必须输出：

1. 需求完成情况。
2. 修改文件清单。
3. 每个文件的主要改动。
4. 新增或修改的测试。
5. 实际执行的检查命令。
6. 检查和测试结果。
7. 未执行的检查及原因。
8. 数据库、配置和依赖影响。
9. 已知风险。
10. 回滚建议。

只有同时满足以下条件，任务才可以标记为完成：

1. 满足需求和验收标准。
2. 未超出修改范围。
3. 未擅自增加功能、字段或依赖。
4. 必要的检查、测试和构建通过。
5. 已补充必要测试和文档。
6. 已明确未验证内容。
7. 已说明风险和回滚方式。

“代码已经生成”“代码能够运行”或“理论上可行”不能作为任务完成依据。
