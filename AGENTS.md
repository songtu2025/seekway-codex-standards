# Codex 项目开发规范

- 规范来源：SEEKWAY Codex 开发规范
- 规范版本：V1.4.0
- 最后更新：2026-09-04

本文件只保留始终生效的核心规则和专项规范索引；“必须”“不得”和“禁止”均为强制要求。

## 1. 优先级与规则加载

1. 系统和平台约束、法律合规、数据安全、权限与生产边界不得被项目文档或普通需求覆盖。
2. 在上述边界内，依次遵守用户明确目标和验收标准、从项目根目录到当前目录的 `AGENTS.md`、任务命中的专项规范、项目现有实现和默认规则；更接近当前目录的 `AGENTS.md` 更具体。
3. 专项规范是本文件的强制扩展；文件缺失、内容冲突或不适用于当前项目时必须说明并确认，不得猜测执行。
4. 需求、业务规则或影响范围不明确时必须先确认，不得自行编造。

| 触发条件 | 必须读取 |
| --- | --- |
| 了解业务边界、架构、启动或部署方式 | `README.md` 及其明确引用的文档 |
| 实施代码、调整结构、配置、依赖、部署或项目文档 | `docs/codex/workflow.md` |
| 新增、修改、重构或评审任何代码 | `docs/codex/code-quality.md` |
| 修改 Python、FastAPI、数据库或迁移 | `docs/codex/backend.md` |
| 修改 React 或 TypeScript | `docs/codex/frontend.md` |
| 修改 Web 界面 | `docs/codex/frontend.md` 和 `docs/web-ui-standard.md`；后者不存在时读取 `templates/web-ui-standard-template.md` 并报告缺失 |
| 制定验证方案、执行检查或输出完成报告 | `docs/codex/verification.md` |
| 编写或核对任务验收标准 | 当前任务说明；采用仓库模板时再读取 `templates/task-template.md` |

## 2. 始终生效的工作边界

1. 修改前必须阅读相关代码和测试，检查调用链、现有实现与可复用能力，并复述需求和验收标准。
2. 非简单任务必须先说明修改文件、风险、验证方案及数据库、认证、权限、配置、依赖和部署影响，获得确认后再实施；简单且边界明确的任务可以直接实施。
3. 只修改完成当前任务所必需的内容，不得自行增加功能、字段、接口、业务规则或无关重构。
4. 未经确认不得更换技术方案、增加生产依赖或修改公共模块、认证、权限、数据库和部署配置。
5. 优先复用现有实现，不得复制业务规则、提前设计未来能力或保留本次修改产生的失效代码。
6. 不得主动读取、输出、提交或传播真实敏感值；允许使用不显示命中值的扫描工具，意外发现敏感信息时必须停止相关处理并提醒用户。
7. 所有进入仓库的代码均适用相同质量规则，不区分人工编写或自动生成。

## 3. 验证与完成

项目必须在 README 中声明实际可执行的 Windows 和 Linux/CI 检查命令；本规范不假定统一脚本名称。

1. 必须按 `docs/codex/verification.md` 执行与实际影响有关的检查，并如实报告通过、失败、未配置和不适用项。
2. 不得通过删除测试、修改正确预期、关闭规则、忽略错误或隐藏失败来通过检查。
3. 只有满足验收标准、未超出范围、必要检查通过且未降低代码健康度时才可标记完成；“代码已生成”或“理论可行”不代表完成。
