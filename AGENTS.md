# Codex 项目开发规范

- 规范来源：SEEKWAY Codex 开发规范
- 规范版本：V1.3.0
- 最后更新：2026-09-04
- 默认开发环境：Windows
- 代码托管平台：GitHub
- 生产环境：阿里云 ECS

本规范用于约束 Codex 在公司项目中的需求分析、方案设计、代码修改、测试验证和结果汇报行为。

本规范中的“必须”“不得”和“禁止”均为强制要求。

## 1. 技术基线

默认技术栈：

### 前端

- React
- TypeScript
- Vite

### 后端

- Python
- FastAPI
- Pydantic
- SQLAlchemy
- Alembic
- pytest
- Ruff

### 部署

- Docker
- Docker Compose
- Nginx
- 阿里云 ECS

未经明确批准，Codex 不得：

1. 更换前端或后端框架。
2. 更换数据库访问或迁移方案。
3. 改变现有部署方式。
4. 引入新的基础设施组件。
5. 新增生产依赖。

## 2. 标准项目结构

新项目默认采用前后端分离、同一仓库管理的结构：

```text
project-name/
├── AGENTS.md
├── README.md
├── .env.example
├── .gitignore
├── compose.yaml
├── docs/
├── frontend/
│   ├── package.json
│   ├── src/
│   └── tests/
├── backend/
│   ├── pyproject.toml
│   ├── alembic.ini
│   ├── app/
│   ├── migrations/
│   └── tests/
├── deploy/
├── scripts/
│   ├── check.ps1
│   └── check.sh
└── .github/
    ├── workflows/
    ├── ISSUE_TEMPLATE/
    ├── pull_request_template.md
    └── CODEOWNERS
```

Codex 必须遵守：

1. 前端代码放在 `frontend`。
2. 后端代码放在 `backend`。
3. 项目文档放在 `docs`。
4. 数据库迁移放在 `backend/migrations`。
5. 部署配置放在 `deploy` 或根目录明确位置。
6. GitHub 配置放在 `.github`。
7. 未经批准不得改变一级目录结构。
8. 项目不需要的二级目录可以省略。
9. 不得提交临时文件、日志、数据库文件和真实业务数据。

维护历史项目时，应优先保持现有结构和兼容性，不得仅为了符合模板而进行无业务收益的全量重构。

## 3. 开始开发前

Codex 在修改代码前必须：

1. 阅读根目录和相关子目录中的 `AGENTS.md`。
2. 阅读 README、需求文档、相关代码和测试。
3. 检查相关调用链和现有实现方式。
4. 复述需求和验收标准。
5. 列出计划修改的文件及修改内容。
6. 说明是否涉及数据库、认证、权限、配置、依赖或部署。
7. 识别潜在风险。
8. 给出测试和验证方案。

涉及 Web 界面时，还必须：

1. 阅读 `docs/web-ui-standard.md`；不存在时采用本规范第 7 节的公司默认规则。
2. 检查项目已有设计令牌、组件库、页面布局和相似页面。
3. 明确设计来源、目标视口和加载、空数据、失败、禁用、成功等页面状态。

未完成上述分析前，不得直接修改代码。

需求不明确时，不得自行编造业务规则。

## 4. 修改范围

Codex 必须遵循最小修改原则。

Codex 必须：

1. 只修改完成当前任务所必需的文件。
2. 保持现有业务规则、接口和兼容性。
3. 优先复用已有代码和实现模式。
4. 新增或修改业务逻辑时补充必要测试。
5. 发现无关问题时只记录，不顺带修改。

Codex 不得：

1. 自行增加功能、页面、接口或业务规则。
2. 自行增加需求中没有的业务字段。
3. 进行与当前任务无关的重构。
4. 修改无关文件。
5. 擅自删除已有功能、接口、字段或数据。
6. 擅自修改公共模块、认证、权限或部署配置。
7. 为“以后可能使用”提前增加代码、字段或组件。
8. 在多个位置复制相同业务规则。

## 5. 代码设计与可维护性

Codex 设计和修改代码时必须：

1. 保持高内聚、低耦合，每个模块只承担清晰且相关的职责。
2. 保持依赖方向清晰，避免循环依赖和跨层调用。
3. 优先保证代码可读性，使用准确命名和简单控制流程。
4. 控制函数、类和组件的职责范围，复杂逻辑必须合理拆分。
5. 隐藏模块内部实现，仅暴露完成协作所需的最小接口。
6. 避免重复业务规则，但不得为了复用而过早抽象。
7. 优先遵循项目已有结构和实现方式，保持代码风格一致。
8. 代码设计必须便于测试、修改和问题定位。
9. 不得硬编码密码、密钥、服务地址、端口、文件路径和环境相关参数，应通过配置或环境变量管理。
10. 业务状态、类型、阈值和重复使用的固定值应定义为具名常量、枚举或集中配置，不得散落在代码中。
11. 仅在值具有明确含义、不会随环境或业务变化且只在局部使用时，才允许直接使用字面量。
12. 每个代码文件应保持职责集中；当文件包含多个相互独立的职责，或已经明显影响理解、定位、测试和修改时，必须合理拆分。
13. 文件拆分应按照业务职责和模块边界进行，不得仅为减少代码行数而机械拆分。
14. 拆分后应保持依赖方向清晰和对外接口稳定，不得引入循环依赖、重复逻辑或无业务收益的文件碎片。
15. 维护历史长文件时，仅处理与当前需求相关的部分；如需进行大范围拆分，必须先说明范围、收益和风险并获得确认。

### 代码规模、复杂度与拆分规则

可维护性以职责聚合、依赖方向、复杂度、可分析性、可修改性和可测试性为主要判断依据。代码行数只能作为预警信号，不得单独作为代码质量结论或任务完成条件。

Codex 必须遵守：

1. 每个模块、类、组件和函数必须具有清晰职责；存在多个能够独立变化的职责时，应按业务边界拆分。
2. 代码文件超过 500 行时，必须检查职责数量、依赖关系、复杂度和测试方式；仅有行数较多不得作为强制拆分理由。
3. 代码文件超过 800 行时，必须在实施方案中说明当前职责、复杂逻辑位置、拆分可行性和测试影响。
4. 新建代码文件预计超过 1000 行，或本次修改会使原本不超过 1000 行的文件增长到 1000 行以上时，必须在实施前提出拆分方案；确实不宜拆分时，说明原因并获得确认后方可继续。
5. 已有超长文件不阻塞局部缺陷修复和必要变更；本次修改不得继续加入无关职责，能够独立的新能力应放入职责明确的模块。
6. 函数应只完成一个明确目标。函数超过 50 行时必须检查复杂度和职责，但不得仅为减少行数提取无业务含义的辅助函数。
7. 函数默认质量阈值为：圈复杂度不超过 10、分支数不超过 12、语句数不超过 50、嵌套块不超过 5、位置参数不超过 5。项目已有认知复杂度检查时，默认不超过 15。
8. 超过函数质量阈值时必须优先简化控制流程、拆分职责或调整数据结构；确有合理原因无法降低时，必须在实施方案和完成报告中说明。
9. React 页面和组件应按数据获取、业务状态、表单、弹窗、表格配置和独立页面区域检查职责；即使行数较少，混合多个独立职责时也必须拆分。
10. 自定义 Hook 只封装一组内聚且可复用的状态或副作用，不得通过把复杂逻辑整体搬入 Hook 来规避组件复杂度。
11. Router、Service、Repository 和展示组件必须保持职责边界，不得为了缩短文件而增加无业务意义的转发层。
12. 拆分后不得产生循环依赖、重复业务规则、只转发调用的空壳模块、过度参数传递或难以追踪的文件碎片。
13. 一个任务或代码评审应保持为一个自包含的目标。本次代码变更量（新增和修改，不含纯删除）接近或超过 1000 行时，必须优先拆分为可独立验证的阶段；无法拆分时应在实施前说明原因并获得确认。
14. 功能修改与大范围重构原则上分开实施；只有重构是完成当前需求的必要前提时，才可纳入同一任务，并必须说明范围和回归风险。
15. 采用渐进治理：新增和修改的代码不得降低整体代码健康度，不要求为了局部需求一次性重构全部遗留代码。
16. 完成修改后，必须检查本次涉及文件的职责、规模和函数复杂度，并如实报告触发的预警、处理结果和未处理原因。

## 6. Python 与 FastAPI

Codex 生成或修改的 Python 代码必须：

1. 符合 PEP 8。
2. 使用 Ruff 进行格式和静态检查。
3. 使用清晰的英文变量名、函数名、类名和文件名。
4. 普通注释和 docstring 使用中文。
5. 为公共函数和复杂业务逻辑提供类型注解。
6. 保持 FastAPI 路由简洁。

后端基本职责关系：

```text
API Router → Service → Repository → Database
```

职责约定：

- `api`：接收请求、校验参数、调用服务、返回结果。
- `schemas`：定义请求和响应结构。
- `services`：处理业务规则和业务流程。
- `repositories`：处理数据库访问。
- `models`：定义数据库模型。
- `core`：管理配置、日志、安全等公共能力。

Codex 不得：

1. 在 FastAPI 路由中堆积复杂业务逻辑。
2. 在路由中直接编写大量数据库查询。
3. 使用裸 `except`。
4. 吞掉异常。
5. 使用拼音或无意义缩写命名。
6. 通过关闭 Ruff 或类型检查规则掩盖问题。

## 7. React、TypeScript 与 Web 界面

Codex 生成或修改的前端代码必须：

1. 使用 React 函数组件和 Hooks。
2. 遵循 React 和 TypeScript 主流开发规范。
3. 使用清晰的英文变量名、函数名、组件名和文件名。
4. 普通注释、JSDoc 和 TSDoc 使用中文。
5. 将 API 请求集中放在 Service 层。
6. 将复杂业务逻辑放入 Hook、Service 或业务模块。
7. 正确处理加载、失败、空数据和成功状态。
8. 公司内部和后台业务系统遵循 `templates/web-ui-standard-template.md` 的默认规则；项目应将其复制为 `docs/web-ui-standard.md` 并填写项目级差异。
9. 界面规则的优先级为：用户明确提供的设计稿或验收标准、项目界面规范、项目现有设计系统、公司默认界面规范。
10. 复用项目已有设计令牌和基础组件，不得在业务页面中随意新增颜色、字号、间距、圆角、阴影和层级值。
11. 使用语义化设计令牌表达用途，例如 `text-primary`、`surface-page`、`border-default` 和 `status-danger`，不得用具体颜色名称表达业务含义。
12. 每个数据页面必须处理加载、空数据、请求失败和正常数据状态；提交操作必须处理处理中、成功和失败状态。
13. 默认面向桌面端内部业务系统，采用深森林绿、米白背景、克制且信息密集的 SEEKWAY“运行驾驶舱”视觉方向；优先保证信息层级、操作效率和数据可读性，不得套用官网或营销页的大标题、大留白和装饰性视觉样式。
14. 新页面应复用统一页面骨架，包括导航、页头、内容区、筛选区、数据区和反馈区域；相同功能在不同页面的位置和交互应保持一致。
15. 表单控件必须有可见标签和清晰错误信息；危险操作必须说明影响并进行确认。
16. 交互必须支持键盘操作和可见焦点，优先使用原生 HTML 语义；自定义复杂组件应遵循 WAI-ARIA Authoring Practices。
17. 默认达到 WCAG 2.2 AA：正文文字对比度不低于 4.5:1，大号文字和非文本界面元素不低于 3:1，不得只用颜色传达状态。
18. 页面在项目声明的目标视口下不得出现意外横向滚动、内容遮挡或不可操作区域；数据表格确需横向滚动时应限制在表格容器内。
19. 动效只用于状态变化和空间关系说明，并尊重 `prefers-reduced-motion`。

Codex 不得：

1. 滥用 `any`。
2. 使用 `@ts-ignore` 掩盖错误。
3. 无理由使用非空断言。
4. 将大量请求、状态和业务逻辑堆积在单个组件中。
5. 在多个位置重复维护同一份状态。
6. 使用前端逻辑代替后端权限校验。
7. 将金额、库存和权限等关键结果以前端计算结果为准。
8. 未经批准引入新的 UI 组件库、图标库、CSS 框架或字体依赖。
9. 为追求“更好看”擅自改变业务流程、字段、权限、默认值或操作结果。
10. 使用 Emoji 代替业务图标，或混用多个图标体系。
11. 在同一页面中创建多个同等强调的主操作。
12. 隐藏焦点样式，或使用不可通过键盘操作的伪按钮、伪链接。
13. 仅实现正常数据状态而遗漏加载、空数据、失败、禁用或提交中状态。

## 8. 注释规范

注释应解释：

- 业务规则
- 设计原因
- 边界条件
- 特殊处理
- 兼容性要求
- 风险原因

不得添加简单重复代码含义的无效注释。

## 9. 数据库规范

Codex 涉及数据库时必须：

1. 使用 Alembic 管理结构变更。
2. 提供升级和回滚方案。
3. 明确说明受影响的表、字段和数据。
4. 保持代码和迁移脚本一致。
5. 对批量操作说明影响范围。

Codex 不得：

1. 直接修改生产数据库。
2. 手工修改生产表结构。
3. 擅自删除表、字段、索引或业务数据。
4. 擅自增加需求中没有的业务字段。
5. 未经确认增加系统字段、派生字段或管理字段。
6. 使用浮点数存储或计算金额。
7. 自动执行生产数据库迁移。

生产数据库迁移只能由部署负责人执行。

## 10. 验证要求

开发人员默认在 Windows 环境使用 Codex。

Codex 提供的本地命令必须兼容 Windows PowerShell。

推荐 Windows 检查入口：

```powershell
.\scripts\check.ps1
```

Linux、GitHub Actions 或服务器检查入口：

```bash
./scripts/check.sh
```

两套脚本的检查内容必须保持一致。

Codex 完成修改后，至少应执行：

### 前端

- 格式检查
- ESLint
- TypeScript 类型检查
- 自动化测试
- 生产构建
- 涉及界面时，在项目声明的目标视口进行视觉检查
- 涉及交互时，使用键盘完成关键操作路径

### 后端

- Ruff 格式检查
- Ruff 静态检查
- Python 类型检查
- pytest
- Alembic 迁移检查

### 通用

- Docker Compose 配置检查
- 敏感信息检查
- 本次涉及文件的职责和规模检查
- 函数圈复杂度、分支数、语句数、嵌套和参数数量检查
- 代码重复、死代码和循环依赖检查

项目已有工具能够执行上述检查时，必须使用项目工具和既有配置；项目没有相应工具时，不得擅自增加生产依赖，应进行人工检查并报告未自动验证的内容。

Codex 必须如实报告：

1. 实际执行的命令。
2. 已通过的检查。
3. 失败的检查。
4. 未执行的检查。
5. 未执行的原因。

Codex 不得为了通过检查而：

- 删除测试
- 修改正确的测试预期
- 关闭检查规则
- 忽略类型错误
- 吞掉异常
- 隐藏失败结果

## 11. 安全与生产边界

Codex 不得读取、生成、提交或传播真实敏感信息，包括：

- 真实密码、Token、AccessKey、SecretKey 和私钥
- 生产环境变量的真实值
- 未脱敏的真实业务数据
- 包含真实敏感信息的数据库文件、日志、上传文件和导出结果

Codex 可以读取和处理：

- 认证相关的代码、字段名和配置结构
- 不包含真实凭据的 `.env.example` 和占位值
- 测试数据、模拟数据和已经脱敏的数据
- 用户明确提供且不包含真实敏感信息的需求附件、示例文件和设计资料

发现真实敏感信息时，应停止处理相关内容并提醒用户，不得在回复中复述其值。

## 12. 项目文档

Codex 创建或维护项目时，至少必须维护：

### README.md

应包含：

- 项目用途和适用范围
- 明确不做的事项
- 技术栈和系统架构
- 关键业务规则
- 本地启动方式
- 测试和构建方式
- 部署文档入口

### AGENTS.md

应包含：

- 项目业务边界
- Codex 修改规则
- 禁止修改的内容
- 必须执行的检查
- 数据和 Git 安全要求

重要业务规则不得只存在于聊天记录或个人记忆中。

## 13. 完成报告

Codex 完成任务后必须输出：

1. 需求完成情况。
2. 修改文件清单。
3. 每个文件的主要改动。
4. 本次涉及代码的职责、规模和复杂度检查结果，以及触发预警后的处理情况。
5. 新增或修改的测试。
6. 实际执行的检查命令。
7. 检查和测试结果。
8. 涉及界面时，报告视觉、目标视口、页面状态、键盘操作和可访问性验证结果。
9. 未执行的检查及原因。
10. 数据库、配置和依赖影响。
11. 已知风险。
12. 回滚建议。

只有同时满足以下条件，任务才可以标记为完成：

1. 满足需求和验收标准。
2. 未超出修改范围。
3. 未擅自增加功能、字段或依赖。
4. 必要的检查、测试和构建通过。
5. 已补充必要测试和文档。
6. 已明确未验证内容。
7. 已说明风险和回滚方式。
8. 新增和修改的代码未降低整体代码健康度，触发的规模和复杂度预警均已处理或说明。

“代码已经生成”“代码能够运行”或“理论上可行”不能作为任务完成依据。
